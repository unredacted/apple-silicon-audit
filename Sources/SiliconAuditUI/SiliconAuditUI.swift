import SiliconAuditCore
import SwiftUI

/// Shared SwiftUI layer used by every app target. Entry point: `AuditRootView`.
public enum SiliconAuditUI {
    public static let version = SiliconAuditCore.version
}
